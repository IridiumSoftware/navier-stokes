# Lean build artifacts (lean-mathlib: lake build creates .lake/; lean writes *.olean)
.lake/
build/
*.olean
*.ilean
*.out.txt
# nrs/ rung outputs are COMMITTED evidence artifacts (A3 discipline), not build transients
!nrs/*.out.txt
