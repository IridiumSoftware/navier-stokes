# Cross-audit — 2026-06-09

**Scope:** full A0–A7 cross-audit of the NS-obstruction program, run after a long gap (prior audit
`audit_2026-06-04.md`, +5 days, ~50 version bumps). Method: three parallel read-only audit agents over
`SPEC.md` (1185 lines), `artifact_registry.md`, `dashboard.md`, `CLAUDE.md`, `TEST_SPEC.md`,
`changelog.md` (v0.6.1), the prior audit, and `git ls-files`. Nothing was modified during the audit pass;
fixes (below) are applied separately and logged in `changelog.md`.

## Headline

**Scientific integrity intact; the drift is all bookkeeping.** Per-entry status tally (authoritative,
parsed not grepped): **`:proved`=0, `:verified`=0, `:benchmarked`=0, `:tested`=12, `:cited`=10,
`:argued`=7, `:open`=4, `:falsified`=1 — 34 entries.** No entry violates the evidence-type→status rule;
**no regularity-/blowup-bearing entry sits above `:argued`** (NS-001/046/048/049 `:open`;
NS-002/013/034/036 `:argued`); the only regularity entries above that are `:cited` external theorems. **The
"distance to the prize UNTOUCHED / `:proved`=0" invariant holds.** (The raw `grep ':proved'` count of 39 is
inflated by prose — "`:proved`=0" disclaimers — not entry statuses.)

Ground-truth counts: **34 spec entries** (NS-001..013, 020..025, 030..040, 045, 046, 048, 049),
**33 registry rows** (NS-049 missing), changelog at **v0.6.1 / 2026-06-09**.

---

## Findings

### BLOCKERS

**B1 — NS-049 has no `artifact_registry.md` row (A1 coverage).** NS-049 (Lockwood "Singularity Surgery") is
a full SPEC entry (`:open`, Tier C0/C1, evidence `external working papers`, with tracked probe
`scripts/ns049_anisotropy_defect_probe.jl` + companions). The registry stops at NS-048, so its own closing
self-claim "every NS-ID has a row" is false. → **Fix: add the NS-049 row.**

**B2 — NS-050 is committed but unspec'd (A6 orphan).** `scripts/ns050_modulation_witness.jl` (+`.out.txt`)
and three docs (`ns050_modulation_type2_scope.md`, `ns050_dss_modulation.md`,
`ns050_modulation_witness_companion.md`) were committed (5966eeb) this session, but NS-050 is absent from
SPEC.md, the registry, and TEST_SPEC.md — substantive tracked work with no ledger home. The docs marked
NS-050 "proposed, not written"; the commit made that an orphan. → **Fix: write the NS-050 `:open` entry +
registry row** (Scope: PDE-analysis; the CLM witness as a within-truncation sub-probe).

**B3 — Entry-count drift, 30/32/33 vs actual 34 (A5).** `dashboard.md:247` ("30 entries"),
`dashboard.md:44` ("SPEC 32 entries"), `CLAUDE.md:20` ("30 ledger entries"), `SPEC.md:14` ("(33 entries.)",
breakdown omits NS-049). This is **F1 from 2026-06-04 re-opened** — point-fixed then, drifted again because
no standing "update count on each new entry" step exists. → **Fix: set all to 34; add NS-049 to the SPEC
breakdown.**

**B4 — Status stamps badly stale (A5).** `CLAUDE.md:19` "Status (2026-06-04, v0.1.42)" — 5 days / ~50
versions behind (now v0.6.1); the whole Lean/Mathlib formalization ladder + citation-disproof arc are
invisible to CLAUDE.md. `dashboard.md:142` header "v0.1.39, 2026-06-02" (lags its own newer prose above it).
`SPEC.md:3` still "v0.1.0 (2026-05-31)". **F3 from 2026-06-04 re-opened.** → **Fix: refresh all three
stamps.**

### WARNINGS

**W1 — `metal/B_abcpert_512.txt` dangling glob member (A3).** NS-040's glob
`metal/B_{helical,helicalc,abcpert}_{256,512}.txt` (SPEC.md:889, registry:41) over-specifies: the
`abcpert_512` member doesn't exist (only `abcpert_256` does; the other 5 members exist). Non-load-bearing
(abcpert is a near-laminar supporting note, not the matched pair). → **Fix: narrow the glob.**

**W2 — NS-045 `:tested` but no TEST_SPEC row (A6).** TEST_SPEC tops out at T-22/AT-7; the 12th `:tested`
entry NS-045 (`ns045_helicity_mechanism.jl`) has no T-row, while its siblings NS-038/039/040 do (T-11/12/13).
**F5 from 2026-06-04 recurring** (the doc's own TEST_SPEC-lockstep invariant). → **Fix: add the NS-045 T-row.**

**W3 — closure-v5 / Q_102 substrate provenance missing (A7).** `scripts/closure_q102_richgate.jl:17` loads
closure-v5 canonical data (the Q_102 object + knfamily/mr_gate/triad_rotation/offgas feeds) by hard-coded
absolute path into NS-022/023/024 (SPEC.md:443) and the NS-037 import (SPEC.md:742), with **no
`substrate_source:` field and no cross-project verification artifact** — contra the codified A7 rule.
**Mitigated:** all four entries are firewalled `Scope ≠ PDE`, `:proved`=0, so this is provenance *hygiene*,
not evidence integrity, and does not touch any prize claim. (NS-049 by contrast carries an exemplary
`substrate_source: external@Lockwood-2026:…` at SPEC.md:357 — the pattern to copy.) Secondary: the
navier-stokes CLAUDE.md does not itself define the `substrate_source` field / A7 step (that lives in the
umbrella TCE CLAUDE.md). → **Fix: add `substrate_source` fields to NS-022/023/024/037, verification-artifact
marked pending.**

**W4 — `dashboard.md:370` stale "No `Project.toml`/lockfile yet" (A0).** Contradicted by the repo —
`Project.toml` + `Manifest.toml` both present. → **Fix: remove/correct the line.**

**W5 — DESIGN.md materially stale (A0 / F2 regressed).** DESIGN.md (2026-06-04) lacks NS-045/046/048/049,
the MDAGC synthesis, the Lean ladder, and the disproof arc — a full arc behind, the same F2 drift fixed last
audit. → **Route to priority stack** (DESIGN sync is batched per CLAUDE.md; too large to do well inside the
audit without derailing the NS-050 b/c work — explicitly deferred, not forgotten).

### NOTES

- **N1 — A4 clean / prize intact.** 0 `:proved`; the `algebraic + computed` entries (NS-034/036/037) are
  correctly held at `:argued` (each carries a `computed` component + conjunctive-claim disclaimer). No
  regularity entry above `:argued`. ✓
- **N2 — A2 otherwise clean.** Zero Logic/Status/evidence disagreements among the 33 shared IDs (NS-049
  uncheckable until B1). NS-047 correctly folded into NS-046 (no entry, by design); NS-041/042/043/044
  never existed.
- **N3 — Recurrence pattern.** B3/B4/W5 are all *recurrent staleness*: F1/F2/F3 from 2026-06-04 were
  point-fixed and drifted again within 5 days. The audit's value is partly in noting that point-fixes don't
  stick — see "Standing recommendation."
- **N4 — Orphan utility scripts (intentional).** `scripts/bench_threads.jl`,
  `scripts/grok_scale_invariant_probe.jl` — utilities with no NS-ID, referenced only in changelog/companions.
  Acceptable. The 6 `active_turbulence_*.jl` are correctly in SIM_SPEC (AT-1..7), not SPEC.
- **N5 — dashboard AT header `AT-1..4` lags its own `AT-1..7` body** (dashboard:101 vs 120–139; phenomenology
  track, low severity). → folded into the dashboard refresh.

---

## Disposition

**Fixed this session (B1, B2, B3, B4, W1, W2, W3, W4, N5):** registry NS-049 row; NS-050 entry + registry row;
counts → 34 across SPEC/dashboard/CLAUDE; status stamps refreshed; metal glob narrowed; NS-045 TEST_SPEC row;
closure-v5 substrate_source fields (verification pending); stale lockfile line; dashboard AT header.

**Priority stack (W5):** DESIGN.md sync with the NS-045..050 + Lean-ladder + disproof arc (batched).

## Standing recommendation (to stop F1/F2/F3 recurrence)

The count + status-stamp + DESIGN drift has now recurred across two audits. The durable fix is a
**checklist line in the "large session" close-out**: *when an NS-ID is added, in the same commit update (i)
the SPEC header count, (ii) the dashboard count + status stamp, (iii) the registry row, (iv) TEST_SPEC if
`:tested`.* Recorded here and (fix-pass) added to CLAUDE.md's session-closeout so it is enforced, not just
audited.
