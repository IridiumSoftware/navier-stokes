# Cross-audit — 2026-06-13

**Scope:** full A0–A7 cross-audit, the first since `audit_2026-06-10.md`. **Trigger:** substantial new
work integrated since the last audit — four SPEC entries (NS-051 formalization ladder, NS-052 Go-Map
cross-repo port, NS-053 (d,α) continuation boundary, NS-054 CFS-admissibility reduction; SPEC 35→39),
the ancient-Liouville litmap §4 (all three edges executed), and the `docs/citation_tiers.md` rows that
came with them. **Method:** direct read of `SPEC.md`, `artifact_registry.md`, `dashboard.md`, the local
`CLAUDE.md`, `TEST_SPEC.md`, `changelog.md` (now v0.15.41), the pre-commit ledger guard, and `git`, plus
file-existence and substrate-provenance spot checks on the four new entries.

## Headline

**Scientific integrity intact; the only drift was the dashboard lagging the two newest entries.** The
pre-commit ledger guard (installed after the 2026-06-10 audit as the durable fix for recurring count/stamp
drift) did its job — coverage (39 entry-headers == 39 registry rows) and the SPEC↔CLAUDE.md stamp parity
held automatically through every commit this stretch. So the semantic checks the guard *can't* see were
the whole audit surface, and they came back clean: SPEC↔registry parity (A2), file existence (A3),
status-honesty (A4), and test-spec coverage (A6) all PASS for NS-051..054. `:proved`=0 is genuine across
all 39 rows; no regularity/blowup-bearing entry sits above `:argued` (NS-013 `:argued`; NS-046/048/050/053
`:open`; NS-054 `:falsified`; the `:cited` rows are external theorems). The firewall holds. The four
findings below are all low-severity bookkeeping, all fixed this pass.

## Findings (all FIXED this pass)

### A0/A1 — NS-053 and NS-054 were absent from `dashboard.md`
The dashboard's priority board (P1–P6) and open-items table carried NS-046/048/050/051 but had **no row for
NS-053** (a live `:open` forward-target with active witnesses — the (d,α) continuation boundary) and **no
note of NS-054** (the `:falsified` CFS-admissibility reduction). A reader of the dashboard alone would not
know NS-053 exists. → **FIXED:** added **P7 (NS-053)** to the priority board (with the litmap §4 completion
folded in); added NS-054's closure and the litmap-§4-executed status to the open-items table.

### A5 — Dashboard version stamps stale
"Status summary (v0.11.1, 2026-06-10)" and the audit-note "SPEC **v0.12.0**" both lagged SPEC.md's actual
**v0.15.0** by three minor versions (NS-052/053/054 + the v0.13/0.14/0.15 stamp bumps landed since). Counts
were correct everywhere (39 entries / 39 rows — the guard enforces that), so this was a label-only lag. →
**FIXED:** both stamps bumped to v0.15.0 / 2026-06-13; the audit-note rewritten for this pass.
*(Note on version schemes: SPEC.md's own stamp (v0.15.0) and the changelog's running version (v0.15.41)
are deliberately separate — the concurrent Carleman/Lean session bumps the changelog on every Lean rung
without touching SPEC entries. The guard enforces SPEC-stamp == CLAUDE.md-stamp, the invariant that
matters; the changelog drifting ahead is by design, not a finding.)*

### A7 — NS-053's grok witnesses lacked an inline `substrate_source` pin (minor/hygiene)
NS-052 carries `substrate_source: grok-test@241bc69`; NS-053 referenced its grok witnesses (GO-023/024/025,
round-2 byte-identical) but cited the pin only one hop away, in the round-2 verification doc. Not a
promotion blocker — NS-053 is `:open` and its status rests on the in-repo T-31 probe + tier-checked rails,
not on the grok items being canonical (they corroborate). → **FIXED:** added **`substrate_source:
grok-test@8e0e066`** inline to the NS-053 registry evidence cell.

### A1/A3 — the litmap §4.3 census was not reachable from NS-048's registry row (minor)
The just-committed `scripts/ns048_direction_cone_census.jl` (+`.out.txt`) and the §4.2 sub-table were
traceable via the litmap companion doc, `citation_tiers.md`, and the changelog, but not from their parent
entry (NS-048) in the registry. → **FIXED:** added a litmap-§4-executed line to NS-048's registry row
(§4.1 wall-restatement; §4.2 exact open sub-case; §4.3 census θ\*≈90° N-stable ⇒ CF-family).

## Checks that PASSED (no action)

- **A1 coverage:** 39 SPEC entry-headers == 39 registry rows (guard-confirmed every commit).
- **A2 logic/status parity:** NS-051 (PROGRAM/:tested), NS-052 (RESULT/:tested), NS-053 (FORWARD-TARGET/
  :open), NS-054 (POSSIBILISTIC/:falsified) — class, evidence type, and status match between SPEC and
  registry (NS-054's "POSSIBILISTIC / FORWARD-TARGET" vs registry "POSSIBILISTIC (attack-reduction, closed)"
  is acceptable Key-compression; S-ID is the canonical link).
- **A3 evidence exists:** every file referenced by NS-051..054 + the census exists in the tree.
- **A4 status honesty:** NS-051 `:tested` is *conservative* against its lean-proved+type-checked+algebraic
  evidence — held below `:proved` deliberately because `:proved` is reserved for Scope:PDE and a lean-proved
  *definition* is not a PDE statement (firewall intact); NS-052 `:tested` is A7-licensed (committed
  verification artifact T-30); NS-053 `:open` and NS-054 `:falsified` are honest.
- **A6 test-spec:** T-27/T-28 (NS-051), T-30 (NS-052), T-31 (NS-053) all documented in TEST_SPEC with
  PASS/IN-PROGRESS verdicts and explicit Scope tags (G-5 stands: no CI runner; committed `.out.txt` +
  no-`sorry` Lean compile remain the regression discipline).
- **A7 (closure-v5):** NS-023/024/037 substrate_source fields + the NS-023 cross-build artifact (T-29)
  carried over from 2026-06-10, unchanged. NS-049's Lockwood substrate_source intact.
- **A0 self-audit:** CLAUDE.md reads 39 ledger entries / v0.15.0 (matches SPEC); close-out items (vi)
  citation_tiers-sync and (vii) NS-051-ladder-sync were both honored this stretch (the litmap added
  LRT/CSTY rows to `citation_tiers.md`); the "leave concurrent Lean files untouched" rule held.

## Disposition

**Fixed this pass:** the four findings above (dashboard NS-053/054 board rows + stamps; NS-053 grok pin;
NS-048 litmap census reference). **Carried (unchanged, by design):** the standing `.lake`-hazard
verification debt (GO-011 + AxisymColumnarBridge + CarlemanEssBridge unverified pending an isolated
grok-test Lean build) — out of scope for a bookkeeping audit, tracked in NS-052/NS-048. **No process
change needed:** the ledger guard absorbed the count/stamp class of drift that recurred in the three prior
audits; what remains (a new entry not yet reflected in the dashboard's *prose* board) is below the guard's
structural checks and is the kind of thing this manual audit is for. `:proved`=0; distance UNTOUCHED.
