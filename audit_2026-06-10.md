# Cross-audit — 2026-06-10

**Scope:** full A0–A7 cross-audit, run the day after `audit_2026-06-09.md` and **the day the repo went public**. Method: three parallel read-only agents over `SPEC.md`, `artifact_registry.md`, `dashboard.md`, `CLAUDE.md`, `README.md`, `TEST_SPEC.md`, `changelog.md` (now v0.11.0), the prior audit, and `git`. Trigger: a full new arc (NS-050 instrument: ~13 scripts + 7 docs) landed since yesterday, plus the README/public flip and the concurrent Lean track (v0.8→v0.11).

## Headline

**Scientific integrity intact; bookkeeping drift recurred — and now it's public.** Per-entry tally (parsed): `:proved`=**0**, `:tested`=12, `:cited`=10, `:argued`=7, `:open`=5, `:falsified`=1 — **35 entries, registry 35/35, identical ID sets**. No entry's status exceeds its evidence; **no regularity/blowup-bearing entry sits above `:argued`** except external `:cited` theorems; the Lean ladder is honestly self-labeled "library infrastructure, `:proved`=0 for the PDE." The integrity firewall holds. **But the prior audit's recurrence prediction came true:** the close-out checklist it recommended was never installed, both its deferred items fell off the tracking surface, and a full arc shipped to the **public** repo unledgered while the README points visitors at it.

## Findings

### BLOCKERS

**B1 — The NS-050 instrument arc is unledgered, and the public README advertises it.** ~13 `scripts/ns050_*.jl` (+`.out.txt`) and 7 `docs/ns050_*` companions/scope (the b/c1/c2/c3 instrument arc, the HL `β=2.47` calibration, the validated 2D-wall solver, the mapped-grid ℝ-operators, the profile-reconstruction wall) appear in **no** SPEC entry, registry row, or TEST_SPEC row — the SPEC NS-050 entry still reads "gated at M1 (no profile)," pre-dating all of it. Meanwhile `README.md` (the public front door) headlines the mapped-grid toolkit ("validated to machine precision — `scripts/ns050_mapped_grid.jl`") and "four distinct failure modes (`docs/ns050_*`)." A public visitor is pointed at work the repo's own "no row → no traceable evidence" rule says isn't established. → **FIXED:** SPEC NS-050 entry + Source extended with the full arc; registry NS-050 row extended; **T-25** (mapped-grid V1/V2 machine-precision) + **T-26** (HL `β=2.47` two-point calibration) added.

**B2 — The prior audit's two deferred items never reached the priority stack, and the audit doc self-contradicts on W3.** `audit_2026-06-09.md` §Disposition lists W3 (closure-v5 `substrate_source`) as "Fixed this session" while the same commit/changelog say "Deferred"; SPEC has **1** `substrate_source` (NS-049 only). W5 (DESIGN.md sync) was routed "to priority stack" but the dashboard priority stack never got either item. → **FIXED:** both added to the dashboard priority stack (still deferred, but now *tracked*); the W3↔Disposition contradiction noted here.

### WARNINGS (all bookkeeping; all FIXED this pass unless noted)

- **W-A — dashboard 9 versions stale:** "Status summary (v0.6.2, 2026-06-09)" vs changelog v0.11.0; **zero** mention of NS-050 or of the repo going public. → **FIXED** (stamp + NS-050/public bullet).
- **W-B — dashboard ledger breakdown sums to 30, not its claimed 35** (the 5 FORWARD-TARGETs NS-045/046/048/049/050 were omitted from the itemization). → **FIXED.**
- **W-C — CLAUDE.md status narrative ends at NS-040/NS-032** (no NS-045..050, MDAGC, C0–C5, Lean ladder, disproof arc); stamp 9 versions stale. → **FIXED** (stamp + the close-out checklist; narrative pointer added).
- **W-D — SPEC stamp "v0.6.1 (2026-06-09)" vs its own v0.8.1 content + v0.11 changelog.** → **FIXED.**
- **W-E — SPEC `metal/B_{…,abcpert}_{256,512}` glob over-specifies** (`B_abcpert_512.txt` doesn't exist); registry was narrowed last audit, SPEC wasn't (recurrence of W1). → **FIXED.**
- **W-F (A7) — closure-v5/Q_102 `substrate_source` still missing for NS-022/023/024/037**, and the scrubbed path (`~/Desktop/Research Papers/…/Closure v5`) is now machine-local/**unresolvable for a public visitor** — provenance is *less* traceable publicly. Mitigation unchanged (all four Scope≠PDE, `:proved`=0; hygiene not integrity). → **priority stack.**
- **W-G — the close-out checklist the last audit recommended was never installed**, and the predicted failure (work committed without ledger updates) recurred within 24h at ~13× scale. → **FIXED THIS TIME:** installed in CLAUDE.md.

### NOTES
- **N1 — integrity confirmed:** `:proved`=0 genuine; no regularity-bearing over-claim; counts honest where stated; public `master`=`active`=`origin/master`. README claims verified true.
- **N2 — formalization/ Lean ladder is ledger-invisible** (SPEC/registry/TEST_SPEC have zero hits; all references are in changelog only). Honestly self-disclaimed as infrastructure, and the README firewall protects a visitor — but the strongest-evidence-class artifacts have no NS-ID. → **priority stack** (an INFRASTRUCTURE entry, Scope: formalization, fenced to deny any `:proved`/PDE reading) — **coordinate with the concurrent Lean session, which is live on `LittlewoodPaley.lean`.**
- **N3 — registry footer "Status honesty" paragraph is stale** ("only Scope:PDE rows are cited/open" — contradicted by NS-002/013/034 argued-PDE), and two blank-line table fragments. Cosmetic; footer reworded.
- **N4 — NS-047 dangling:** referenced in `Depends_on` lists but is a refuted-non-entry (by design); a reader following it hits nothing. Low.

## Disposition
**Fixed this pass:** B1, B2 (tracked), W-A, W-B, W-C, W-D, W-E, W-G, N3. **Priority stack:** W-F (substrate_source), W5 (DESIGN.md sync), N2 (formalization ledger entry — coordinate). 

## Standing recommendation — INSTALLED this time
The last audit *recommended* a close-out checklist and it was never added, so the drift recurred. This pass **adds it to `CLAUDE.md`** as an enforced large-session close-out: *when an NS-ID is added or an arc lands, in the same commit update (i) the SPEC entry + header count + stamp, (ii) the registry row, (iii) TEST_SPEC if a check exists, (iv) the dashboard status + stamp, (v) a changelog version entry.* If the next audit finds the same drift again, the checklist isn't being followed and the fix is process (a pre-commit hook), not another point-fix.
