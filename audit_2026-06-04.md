# Cross-audit — 2026-06-04 (omnibus)

Full integrity sweep of the navier-stokes obstruction-program ledger after the Metal/GPU arc
(NS-039 RWC-038 verdict, NS-040 helicity depletion, NS-032 Step-2 @ N=512). Checks A0–A6 adapted
from the project's cross-audit protocol. **Headline: the ledger is sound — 4 findings, all
documentation/count drift, none touching evidence or the firewall.**

## Scorecard

| Check | What | Result |
|---|---|---|
| **A1 Coverage** | every SPEC S-ID has a registry row | **PASS** — 30 SPEC entries, 30 registry rows, identical ID set (NS-001..013, 020..025, 030..040) |
| **A2 Logic/Status parity** | registry status == SPEC status | **PASS** — per-ID join, **0 mismatches** (2 open / 6 argued / 10 cited / 1 falsified / 11 tested in both) |
| **A3 Evidence exists** | every referenced file present | **PASS** — 0 missing files across SPEC/registry/dashboard/TEST_SPEC/DESIGN; 48 `.out.txt`, 28 companions, all source/metal/script paths resolve |
| **A4 Status honesty / firewall** | no status over-claims its evidence; `:proved`=0 | **PASS** — no `:proved` or `:verified` status anywhere; `:tested` ceiling is backed by `computed`/`example-tested`; 58 Scope mentions across 30 entries |
| **A5 Stale counts** | counts agree across SPEC/dashboard | **FAIL (fixed below)** — true count **30**; SPEC line said 27, dashboard header said 29 |
| **A6 Test sync** | spec entries with tests are exercised | **PASS (caveat)** — all referenced scripts exist + recorded `.out.txt`; TEST_SPEC has 8 T-rows; **no automated CI** (research-script repo) ⇒ regression detection is manual-by-rerun, appropriate here but worth noting |
| **A0 Self-audit (DESIGN/CLAUDE)** | docs match observable practice | **2 findings** — DESIGN.md materially stale; CLAUDE.md status stamp stale (details below) |

## Findings

### F1 — A5: entry-count drift (FIXED 2026-06-04)
True count = **30** (30 NS-IDs, 30 registry rows, 30 SPEC status lines). The SPEC count line read
"(27 entries)" and **lacked the POSSIBILISTIC and RESOLVED-DNS categories entirely** — it predated
NS-037 (POSSIBILISTIC) and NS-038/039/040 (RESOLVED-DNS) and was never re-categorized; it also
listed "8 our RESULTS" (should be 6) and "1 ANALYSIS" (should be 2). The dashboard's category
breakdown was already correct (sums to 30) but its header said "29 entries". Authoritative class
tally (from registry col 3): 8 OBSTRUCTION, 6 RESULT/null/falsified, 3 RESOLVED-DNS, 2 RELATED,
2 PROGRAM, 2 DIAGNOSTIC, 2 ANALYSIS, 1 PROBLEM, 1 POSSIBILISTIC, 1 GEOMETRY, 1 CONJECTURE, 1
RESULT(live) = 30. **Fix applied:** SPEC count line rewritten to the 30-summing breakdown;
dashboard header 29→30.

### F2 — A0: DESIGN.md is materially stale (FIXED 2026-06-04)
DESIGN.md §1 (is/is-not) and §6 (success criteria) firewall framing are **correct and consistent**
with SPEC. But §3 ("The live attack: the complex plane") still frames the spectral-truncation work
as the *forward plan* (Burgers → spectral truncation → read obstructions), and §4 ("prior arc")
stops at NS-020..024. DESIGN has **no mention** of the entire **NS-030..040 arc**: the manifold
study (NS-033), scaling-exponent calculus (NS-034), Ryan scope-principle (NS-035), criticality–
Casimir hinge (NS-036), the possibilistic / inverse-Born map (NS-037), the **resolved-DNS boundary
program A→B→C** (NS-038), and the entire **Metal/GPU N=512 track** (NS-039 RWC-038 verdict, NS-040
helicity depletion) + FFTW integration. Per project discipline DESIGN updates are *batched*, so being
behind is allowed — but it is now a full arc + the primary compute track behind. **Fix applied:**
§3's staged plan marked EXECUTED (with the honest INCONCLUSIVE Step-2 result), and a new §7
("What the computation has produced beyond the complex-plane plan, NS-030..040") added covering
the geometric/scaling tour, the possibilistic/inverse-Born map (NS-037), the resolved-DNS boundary
program (NS-038), and the Metal/GPU N=512 track (NS-039/040). Firewall text (§1, §6) unchanged.

### F3 — A0: CLAUDE.md status stamp stale (FIXED 2026-06-04)
CLAUDE.md line 19 read "**Status (2026-05-31, v0.1.0).** Honest baseline: zero progress on the
prize." The *substance* is still correct (`:proved`=0, prize untouched), but the date/version stamp
(2026-05-31 / v0.1.0) is stale — the repo is at **v0.1.42 (2026-06-04)** with 30 entries and the
DNS/Metal arc. "FFTW when spectral methods enter — pin" (line ~101) is now realized. **Fix applied:**
status stamp refreshed; substance ("zero progress on the prize") preserved.

### F4 — A6: no automated CI (NOTE, no action)
The "test suite" is Julia scripts + recorded `.out.txt` + TEST_SPEC T-rows + the Swift GPU solver.
All present and internally consistent, but there is no CI re-running them; regression detection is
manual (rerun the script, diff the `.out.txt`). This is appropriate for a research-script repo and
is *not* a defect — recorded here so the limitation is explicit. A `regression_manifest.md` (per the
CLAUDE.md "forward" note) would be the upgrade if/when the interfaces stabilize.

## Verdict
The ledger's integrity is intact: coverage, parity, evidence-existence, and the `:proved`=0 / Scope
firewall all hold with zero violations. The only issues were count drift (F1, fixed) and doc staleness
(F2 DESIGN — synced; F3 CLAUDE — fixed); F4 (no CI) is a noted-not-a-defect. All four addressed.
Distance to the prize: **UNTOUCHED.**
