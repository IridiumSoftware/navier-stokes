# navigation artifacts (auto-regenerated by the paper-index hooks)
.paper-index.json

# python
**/__pycache__/
*.pyc
.venv/

# julia (commit Project.toml + Manifest.toml when they exist; ignore build junk)
*.cov
/deps/

# os
.DS_Store

# DNS field-state checkpoints (large, regenerable)
scripts/ckpt_*.jls

# Swift build artifacts (Metal track)
metal/probe_mpsfft
metal/*.o
metal/.build/
metal/dns_*512*.bin
