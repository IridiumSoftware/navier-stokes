══════════════════════════════════════════════════════════════════════════════
  dec_repair_sandbox.jl — structure-preserving discrete NS chain complex on 𝕋³
  (tests, on the REAL mesh, the two claims discrete.rtfd rests on + the 2-chain repair cost)
  Scope: discrete model on 𝕋³. NOT the PDE. :proved=0; distance UNTOUCHED.
══════════════════════════════════════════════════════════════════════════════

  (1) STRUCTURE PRESERVATION (the mimetic property + correctness gate):
    N=2:  max|∂₁∂₂| = 0.0e+00   max|∂₂∂₃| = 0.0e+00   ✓ ∂∂=0
    N=3:  max|∂₁∂₂| = 0.0e+00   max|∂₂∂₃| = 0.0e+00   ✓ ∂∂=0
    N=4:  max|∂₁∂₂| = 0.0e+00   max|∂₂∂₃| = 0.0e+00   ✓ ∂∂=0

──────────────────────────────────────────────────────────────────────────────
  (2) BETTI NUMBERS of 𝕋³ vs mesh resolution N  (expect 1,3,3,1 at EVERY N):
──────────────────────────────────────────────────────────────────────────────
    N     b₀     b₁     b₂     b₃       Euler χ
    3     1      3      3      1        0
    4     1      3      3      1        0
    6     1      3      3      1        0
  ⇒ b₁ = 3 at every N: refinement does NOT manufacture new 1-cycle classes. On a
    fixed topology dim H₁ is PINNED — the document's 'refinement proliferates
    non-bounding cycles' is false on the actual mesh. (Confirms NS-020, structurally.)

──────────────────────────────────────────────────────────────────────────────
  (3) 2-CHAIN REPAIR COST  Cost(c₁)=min{‖z₂‖:∂₂z₂=c₁}  (the discrete Seifert surface):
──────────────────────────────────────────────────────────────────────────────
    filament 1-cycle           ‖z‖₂ (total) ‖z‖∞ (peak)  fillable?   
    1×1 planar loop (per.4)    0.8146       0.6636       yes         
    2×2 planar loop (per.8)    1.4235       0.5066       yes         
    3×3 planar loop (per.12)   1.8801       0.4376       yes         
    4×4 planar loop (per.16)   2.1678       0.3771       yes         
    𝕋³-wrapping loop (gen.)    ∞            ∞            NO (H₁ class)
  ⇒ the PEAK repair label ‖z‖∞ does NOT overflow — it actually DECREASES as the loop
    grows (0.66→0.38: the min-norm filling diffuses over the torus, the opposite of the
    document's |a_f|→∞). The L² TOTAL grows only SUB-linearly (below √area=k), bounded
    under volume-preserving stretching. The ONLY infinite-cost cycles are the 3 fixed H₁
    generators — not a proliferating family.

══════════════════════════════════════════════════════════════════════════════
  VERDICT (discrete.rtfd part 2 — the genuine DEC / 2-chain test)
══════════════════════════════════════════════════════════════════════════════
  • DEC operators are structure-preserving (∂∂=0 to machine zero) — the mimetic property
    holds; this is a legitimate discrete-NS substrate.
  • dim H₁(𝕋³)=3 at every resolution ⇒ 'refinement proliferates non-bounding 1-cycles' is
    FALSE on fixed topology. Confirms NS-020 on the actual mesh.
  • The genuine 2-chain repair cost does NOT overflow: peak label DECREASES (0.66→0.38),
    total grows only sub-linearly (below √area), bounded under volume-preserving stretch.
    The 'repair overflow → turbulence' mechanism fails in the chain picture too —
    completing the field/Hodge refutation of part 1.
  • Net: the discrete substrate is real and worth having as a sandbox, but it does NOT
    support the 'dual-closure uplift / the PDE is the wrong model' claim. Scope: discrete
    model on 𝕋³; NOT the PDE; :proved=0; distance to the prize UNTOUCHED.
══════════════════════════════════════════════════════════════════════════════

wrote: /Users/aarongreen/Desktop/navier-stokes/scripts/dec_repair_sandbox.out.txt
