==============================================================================
  NS-046 INTEGRAL / CANCELLATION probe — Kerr-tube worst case (pointwise was NON-UNIFORM)
  R_int = Σ_w(e₃ᵀ∇²p e₃ + ν|∇ω|²) / Σ_w(λ₃²),  w=|ω|²  (≥1 ⇒ depletion+viscous beat self-amp; convention per uniform_domination)
  Scope: resolved DNS truncation; vacuity-capped; :proved=0; CANNOT close NS-046.
==============================================================================
  enstrophy peak Z=2.183 at t=6.00 (N=64, ν=6.25e-04) — analyzing this snapshot

  ── INTEGRAL balance (production-weighted), full domain ──
    R_int (full)                  = 2.419   ≥1: integral DOMINATION holds in this truncation
      pressure-Hessian part        = 1.515 ;  viscous part = 0.903  (sum 2.419)

  ── INTEGRAL over the bad set (top-q by |ω|²) — the CKN-set proxy ──
    top-10.0% : R_int = 2.470
    top-1.0% : R_int = 3.046
    top-0.1% : R_int = 3.808

  ── TRIAD-DEMANDED (verdict 2026-06-11): weight sensitivity + the FEED denominator ──
    P1-C1: is R_int weight-dependent?  R_int = Σ_w(DEPL+VISC)/Σ_w(λ₃²) under each weight:
      w = |ω|² (original)         R_int = +2.419
      w = |ω·Sω| (production)     R_int = +1.533
      w = |λ₃||ω|²                R_int = +1.477
      w = |S|² (strain energy)    R_int = +0.212
    P1-C4: against the FEED ¼(|ω|²−(ω·e₃)²) (the actual λ₃ growth source) instead of λ₃²:
      w = |ω|² (original)         R_feed = +0.984   (≥1 ⇒ depletion+viscous beat the feed)
      w = |ω·Sω| (production)     R_feed = +1.031   (≥1 ⇒ depletion+viscous beat the feed)
    (single snapshot, N=64, one fixture — same unconverged caveat as everything above)

  ── SCALE-resolved (the derivative-loss / marginal-cancellation test) ──
    bin local k_loc=|∇ω|/|ω| ; does R_int DEGRADE toward 1 at small scale (high k)?
    k_loc range        R_int     (production-weighted, per scale band)
    [  5.63,  9.77)     2.486
    [  9.77, 16.93)     1.652
    [ 16.93, 29.36)     1.388
    [ 29.36, 50.90)     1.488
    [ 50.90, 88.26)     1.444
    [ 88.26,153.02)     1.731

==============================================================================
  WITNESS READING (TRIAD-TRIMMED 2026-06-11 — vacuity-capped, NOT the inequality)
==============================================================================
  • R_int(full)=2.42 under w=|ω|² — a WEIGHT-SENSITIVE integral reconciliation (P1-C1 verdict:
    'much of the non-uniformity is a weighting artifact' was REFUTED as over-reach; the supportable
    statement is that enstrophy weighting emphasizes coherent cores — see the weight table above).
  • Scale bins 2.49 → 1.73: NON-MONOTONE; the 'difficulty locus' reading is WITHDRAWN (P1-C2
    REFUTED: k_loc is a sharpness proxy not a spectral scale, and viscous floors force margins ≥1 in a
    regular truncation — the bins may only be rediscovering that regular truncations regularize).
  • Sign convention: machine-verified (ns046_dlambda3_sign_check, algebraic) — pressure coefficient −1.
  • STATUS (P1-C3): unconverged SINGLE-POINT witness — one fixture, one snapshot, one resolution. No
    reading beyond 'suggestive' is licensed before an N-trend + second fixture + multiple times.
  • FIREWALL: resolved truncation, no singular set; :proved=0; distance UNTOUCHED.
==============================================================================
