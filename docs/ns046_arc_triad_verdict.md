# Triad-witness verdict — the NS-046 arc (C1–C3): interpretations REFUTED, data stands

**Seats:** Grok (edge-Φ) + Gemini (synthesis) + ChatGPT (naive). **Date:** 2026-06-06.
**Brief:** `docs/ns046_arc_triad_brief.md`. **Disposition: CONVERGENT 3/3 — the NS-046-arc
*interpretations* over-reached (the 6th time this arc); the within-truncation *data* stands as
phenomenology.** These were committed+pushed claims; the record was corrected (below). `:proved`=0;
distance UNTOUCHED.

## Per-claim verdicts (convergent)

- **C1 (complementarity) — REFUTED as a general law (3/3).** The *random* zero-helicity control does
  NOT show dominant pressure counter-transport (it bursts most); only the special, symmetric
  anti-parallel Kerr-tube IC does. So "zero-helicity ⇒ pressure counter-transport dominates" is false.
  Honest residue: **"the Kerr-tube IC exhibits stronger pressure depletion than the helical run"** —
  IC-specific phenomenology, not a helicity-anti-correlation. (Establishing complementarity would need a
  helicity sweep across many non-symmetric ICs at matched spectra/Re.)

- **C2 (uniform-domination) — DATA holds; the INTERPRETATION is vacuity-capped over-reach (3/3).** A
  regular truncation has no singular set, so its non-uniform *pointwise* conditioned ratios do not bear
  on the *analytic* inequality, which can hold via Besov/integral/cancellation controls that pointwise
  ratios never see (the live `Ḃ⁰_{∞,1}` route). So "makes C2 visible / blocks the analytic reduction"
  over-reaches (the 6th). **Genuine residue (useful):** the probe refutes the *pointwise-domination
  heuristic* — i.e. any closing inequality must go through Besov/integral controls, not pointwise
  domination. The "bulk-enhances/cores-deplete" split is also conditioning-dependent (Q2:
  enstrophy-weighting vs production-conditioning answer different questions; the sign convention itself
  is fine).

- **C3 (target "the irreducible difficulty IS the non-uniformity") — REFUTED as established (3/3).**
  Asserted, not demonstrated; the real difficulty could be elsewhere (e.g. derivative loss at
  marginal-cancellation scaling). Honest form: **"uniform domination on the bad set is *a* natural
  remaining target *if* the Besov-endpoint objection is set aside"** — useful framing, not "*the*
  irreducible difficulty." "Computationally visible" conflates a regular-truncation diagnostic with the
  analytic obstacle.

## The decisive meta-lesson

Probe-first caught the *naive* "pressure dominates" over-reach computationally — but the recorded
*interpretations* (complementarity-law, "blocks the reduction," "the irreducible difficulty") over-
reached again, and only the **adversarial witness** caught them. So even probe-first + careful scoping
was insufficient; the over-reach pattern is persistent. **Six over-reaches this arc** — four by the
adversarial seats (LOW#1, MID, §5-"≡", NS-047-C1), one by the probe (Idea-3 "pressure dominates"), one
by the seats again here. The naive seat (ChatGPT) again made the surface catch ("a finite-Re regular
truncation has no singular set, so why should its intense set identify the obstruction?").

## Disposition / corrections made (committed)

- **No new entry; no status change** (NS-045 `:tested`, NS-046 `:open` unchanged).
- **Record corrected** (this is the honesty step — the over-reaches were committed+pushed):
  NS-045 §extension (complementarity → IC-specific Kerr-tube); the NS-046 uniform-domination note
  (removed "blocks the reduction / C2 visible" → "refutes only the pointwise-domination heuristic; a
  within-truncation diagnostic, not PDE evidence"); the NS-046 pause note + `docs/ns046_target.md` §3
  ("the irreducible difficulty" → "a candidate remaining difficulty, if the Besov-endpoint is set
  aside"). The within-truncation *data* (Idea-3 ratios; the conditioned non-uniformity) stands as
  phenomenology.
- NS-046 remains paused at the (now honestly-scoped) target. `:proved`=0; distance UNTOUCHED.
