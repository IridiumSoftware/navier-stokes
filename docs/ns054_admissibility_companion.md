# NS-054 companion — CFS admissibility on the MDAGC conjunction: the pre-registered move, run on paper

**Date:** 2026-06-12. **Status: the pre-registered KILL CRITERION FIRED — `:falsified` as an
attack-reduction (the stub's predicted exit 1), with a residual: the cross-conjunct coupling inventory.**
Scope: methodology (the executable layer; no PDE claim is made — the over-reach pre-declaration stands).
`:proved`=0; distance UNTOUCHED. **Provenance:** pre-registration stub authored by Aaron
(`~/Desktop/NS-054_stub.md`, 2026-06-12 — incorporated by reference; its RWC-054 and kill criterion are
adopted verbatim). The paper pass below is the stub's "sharp and cheap to settle on paper" question,
settled.

## 1. The pre-registered question (from the stub)

Treat the MDAGC `G1∧…∧G5 ∧ S1,S2 ∧ W1,W2` as a single admissible class `A` and apply the CFS
possibilistic/admissibility filter (the NS-037 machinery, pointed at the *generator class* for the first
time). The stub itself pre-reduced the move: §6 of the MDAGC doc already performs the emptiness reduction
("A empty ⟺ NS-046 ∧ NS-048 closed"), so **the entire non-relabeling content is one question**:

> Is there a **joint obstruction among {G1,G2,G5} × {G3,G4}** beyond the G3∧G4 coupling the doc already
> uses — a configuration each conjunct permits but the combination forbids?

**Kill (pre-registered):** `:falsified` as a reduction if the pass produces no constraint absent from
every individual `Gᵢ` and adds nothing beyond G3∧G4.

## 2. The paper pass — all six cross-pairs

| Pair | What the conjunction says jointly | Verdict |
|---|---|---|
| **G1∧G3** | NRŠ's exclusion is *stated in* `L³` — G3's proof already lives inside G1's norm class. A non-self-similar singularity must still blow `L³`, which is just G1. | **internal** — no joint content beyond the conjuncts |
| **G1∧G4** | the Type-I rescaled ancient solution inherits critical-norm structure, and the **sequential-`L³` Liouville** (**AB Thm 1.2 — attribution CONFIRMED + architecture-read same day**, `docs/ab_sequential_l3_verification_2026-06-12.md`) closes the "`L³`-quiet along a backward sequence" configuration ⇒ jointly: **the ancient limit of an admissible singularity must be critical-norm-wild backward in time** (no `L³`-bounded backward sequence) | **KNOWN-coupling** — the literature's own theorem family; absent from the MDAGC doc's explicit edges → **inventory add**, not a new obstruction |
| **G1∧G5** | Tao's averaged counterexample genuinely blows up (critical norms included) — G1 adds no friction to G5's killed method-class; their joint content is S1 (the σ-gap), already a soft conjunct | **internal** |
| **G2∧G3** | self-similar singularities concentrate at a point (0-D ⊂ 1-D) — CKN never excluded them; G3 did, by different means. No tension, no joint addition | **none** |
| **G2∧G4** | the KNSS ancient-limit extraction **uses CKN-type ε-regularity for its compactness** — this coupling is already cashed *inside* G4's machinery (and stated, unflagged, in the doc's own G4 paragraph: "parabolic rescaling + CKN compactness") | **already-used** (the second pre-existing coupling, alongside G3∧G4) → flag it explicitly |
| **G2∧G5** | CKN is itself an *energy-method* theorem; Tao's averaging shows energy methods cannot reach ∅ ⇒ jointly: **the energy lane's exact ceiling is "≤1-D but not empty"** — the CKN-to-regularity gap is precisely what survives the averaging | **KNOWN-coupling** (folklore; Tao's own framing) → **inventory add** |

**No configuration was found that each conjunct permits but the combination forbids, beyond the known
couplings above.** Every real cross-edge is somebody's existing theorem (ESS/AB-Seregin, KNSS+CKN, Tao's
framing) — exactly the G3-pattern the stub predicted: *membership in this genus is bought with hard
analysis; the lens cannot manufacture it.*

## 3. Verdict — the kill fires (as pre-registered)

**NS-054 is `:falsified` as an attack-reduction:** the admissibility restatement of the conjunction yields
nothing absent from the individual `Gᵢ` plus the known couplings; "is `A` empty?" is **NS-048 (+NS-046),
relabeled** — the stub's predicted exit 1 and the over-reach pre-declaration, confirmed. Per the stub:
*not a failure* — it formally closes "was this move ever worth making" and pins the prize-relevant content
of CFS-admissibility-on-the-no-go-map as exactly NS-046 ∧ NS-048. RWC-054 is **not triggered** (no
"`A` empty/sharpened" claim is made — the opposite). **Caveat held:** this pass records that *this
enumeration found no new joint obstruction*; it is not a nonexistence proof of one — anyone returning must
clear RWC-054 in full.

## 4. The residual (banked where it belongs)

The **cross-conjunct coupling inventory** — the MDAGC doc used G3∧G4 explicitly and G2∧G4 silently; the
map now carries four cited edges (G3∧G4, G2∧G4, G1∧G4 *(tier RESOLVED same day — AB Thm 1.2,
`docs/ab_sequential_l3_verification_2026-06-12.md`)*, G2∧G5) in `docs/ns_blowup_generator_class.md` §5b.
The G1∧G4 edge motivated the litmap's priority line-read, executed the same day: the inventory is now
fully cited.

## 5. Firewall

The stub's over-reach pre-declaration is logged as written: *"admissible class empty ⇒ 3D regularity" is
declined as prize progress in advance.* No status above `:falsified`-as-reduction; Scope: methodology;
`:proved`=0; distance UNTOUCHED.
